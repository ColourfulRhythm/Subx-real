specVersion: "v1"
serviceId: "admin-frontend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-frontend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
